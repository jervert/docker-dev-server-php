{
  "name": "init-run",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:js": "cd src/js && npm install",
    "run:assets": "cd src/node && npm start",
    "start": "npm i && run-p run:*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
